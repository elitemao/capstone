Your job is to split the text into each individual sentence and add sequential number in front of each sentence. Only show the sentences with at least 3 phrases.

You take the article stored in state["article"].


Dissect state["article"] into sentence unit and prefix sequential number to each sentence.

Output:
Store numbered sentences as a list in state["sentence_list"].

